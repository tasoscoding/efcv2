@model IEnumerable<efcv2.Models.Sensori>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SensorDescr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SensorDescr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SensoriID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SensoriID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SensoriID })
        </td>
    </tr>
}

</table>
