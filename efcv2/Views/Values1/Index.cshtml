@model IEnumerable<efcv2.Models.Value>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Value1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeStamp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sensor.SensorDescr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Value1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeStamp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sensor.SensorDescr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ValueID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ValueID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ValueID })
        </td>
    </tr>
}

</table>
